<h1>Editing Round</h1>

<%= render 'form', round: @round %>

<%= link_to 'Show', round_path(@tournament.id,params[:rid]) , class: 'btn btn-primary'  %> |
<%= link_to 'Back', tournament_path(@tournament.id), class: 'btn btn-primary' %>
